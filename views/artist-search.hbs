<div>
    {{#each apiRes}}
    <div id="artist">
        <picture>
            <img src="{{img}}" alt="{{name}}">
        </picture>
        <h2>{{name}}</h2>
        <a href="/albums/{{id}}">View Albums</a>
    </div>
    {{/each}}
</div>