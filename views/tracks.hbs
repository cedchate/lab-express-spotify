<div id="title"> <h2>Title</h2>  <h2>Listen</h2> </div>
<ul id="tracks">
    {{#each apiRes}}
        <li id="track">{{name}} <audio
        controls
        src="{{preview_url}}">
            <a href="{{preview_url}}">
                Download audio
            </a>
    </audio>
    </li>
    {{/each}}
</ul>